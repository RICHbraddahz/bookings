config:
  target: 'http://localhost:3002'
  phases:
    - duration: 60
      arrivalRate: 20
  processor: "./randomId.js"
scenarios:
  - flow: 
    - function: "genRandomId"
    - get:
        url: "/booking/{{ id }}"
    - log: "Request to id {{ id }}"